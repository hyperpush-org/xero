[package]
name = "xero-agent-core"
version = "0.1.0"
edition = "2021"
description = "Reusable Xero agent harness facade and storage contracts"

[lib]
doctest = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
